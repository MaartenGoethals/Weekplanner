<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="activiteit"
            type="be.dekade.weekplanner.data.Activiteit" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/listitem_padding"
        android:layout_margin="4dp">
        <CheckBox
            android:id="@+id/afgewerkt_checkbox"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:button="@null"
            android:background="?android:listChoiceIndicatorMultiple"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:checked="@{activiteit.isAfgewerkt}"/>
        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{activiteit.titel}"
            android:textSize="@dimen/title_size"
            app:layout_constraintStart_toEndOf="@id/afgewerkt_checkbox"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Tanden poetsen" />
        <TextView
            android:id="@+id/uur"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/afgewerkt_checkbox"
            app:layout_constraintTop_toBottomOf="@id/title"
            android:textSize="@dimen/font_size"
            android:text="@{@string/uur_format(activiteit.startuur, activiteit.startminuut)}"
            tools:text="8:00"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>